# C2 — главный агентный исполнитель решения

Назначение пользователя от 23.09.2026: ты главный исполнитель ядра решения HackAlem, а не помощник по исследованию. Это действующее поручение выполнять работу до результата. Подготовка среды, зависимости, численная модель, обучение, историческая оценка, агентные инструменты, CLI и подключение ядра к API — твоя ответственность. Не заканчивай ответом «готов», новым планом или установкой библиотек.

## Запуск и приоритет инструкций

Твоя задача Codex: `01a0cd9e-371b-77a1-a6fb-62c6869e3bb5`, исходное название «Начать новую сессию». Выделенная рабочая копия `D:\HACKALEM\worktrees\c2-ml`, ветка `agent/c2-ml`. Используй этот workdir явно: cwd задачи может оставаться D:\HACKALEM. Репозиторий https://github.com/BAITC-Hacks/hack-c0c67068-qwertys.git. Общая ветка `coord/hackalem-start`.

Сначала проверь branch/status, получи origin и включи актуальную общую ветку обычным merge, сохраняя любую существующую работу. Не переключай ветку и не редактируй файлы в общей копии или копии C1. Не создавай ещё одного исполнителя C2. В течение 3 минут опубликуй CLAIM и `coordination/status/C2.md`: задача/роль, рабочая папка, scope, ближайший результат, blocker, следующий update.

Прочти в этом порядке: AGENTS.md; этот документ; C2_START_NOW.md; ASSIGNMENTS_NOW.md; API_CONTRACT_V1.md; BUDGET_AUTHORIZATION.md; research/C1/READY.md и availability-correction.md; CONTRACTS.md и DATA_MANIFEST.json. Здесь все пути без префикса относятся к coordination/. Затем точечно RESEARCH.md, SCHEDULE.md и GIT_PROTOCOL.md. Не перечитывай многочасовой архив навыков/исследований перед первым запуском. Применяй только навыки под текущую операцию. Этот документ уточняет назначение C2 и заменяет прежнее «исполнитель не идентифицирован»; остальные ограничения сохраняются.

## Что обязано реально работать

1. Подготовка входов из двух официальных CSV и подходящего погодного источника с явными временем, покрытием и происхождением.
2. Воспроизводимая команда обучения, простой baseline и один основной кандидат; оценка на временном holdout до февраля, без подбора на финальном тесте. Выбор по измерениям и остаточному времени, а не названию технологии.
3. Callable прогноз обеих турбин на 24/48 часов: вход issue_time/идентификаторы/горизонт, выход численные строки, версии, provenance и предупреждения по API_CONTRACT_V1. Передай C4 точную сигнатуру, пример и ошибки сразу после определения.
4. Настоящий агентный цикл с вызовами weather/data/model/validate/export, ограниченными повторами, журналом фактических событий и новым run_id при пересчёте. LLM управляет инструментами и объясняет результат; числа и метрики считает код. Не ограничивай итог детерминированным скриптом, если доступен LLM API. При fallback режим явно отличается.
5. CLI и пакет последовательных февральских прогнозов, сохранение перекрывающихся выпусков и документированное правило итогового экспорта. Никаких выдуманных февральских метрик.
6. Совместно с C4/Claude один сквозной путь: UI → API → твои инструменты/модель → график/таблица/экспорт → повторный расчёт. Отказ источника или незагруженная модель не выглядят как успешный расчёт.

## Уже готовые входы и важные ограничения

Интеграционный commit на момент передачи: `73611e6`. C1 создал `src/data/scada.py`, `src/weather/open_meteo.py`, образец контракта, отчёты и тесты. В общей копии 2 unittest проходят; это не проверка будущей модели. Используй готовые модули, не переписывай их параллельно.

Исходные CSV находятся в `D:\HACKALEM\cases`; SHA и названия в DATA_MANIFEST.json. Фактические ряды: 142360/149499 записей, конец 31.01.2026 23:50 несмотря на имена файлов. Февральских labels нет. Подготовленные локальные артефакты C1: `D:\HACKALEM\worktrees\c1-data\artifacts\c1`; сверяй manifest/команды READY и читай как вход, не изменяй чужую копию. В продукте путь конфигурируемый, не жёстко заданный под этот ноутбук.

SCADA UTC+6 — гипотеза. Нормализованная мощность не является подтверждённой величиной в МВт/МВт·ч и не обязана лежать в [0,1]. Не делай произвольный clipping. Для февральского replay реальные февральские лаги недоступны. Измеренная погода будущего и реанализ не заменяют доступный тогда прогноз.

После исправления e48a836 погодный gate использует `run_time + 9h`, `availability_basis=inferred_run_plus_9h`, `provenance_status=unconfirmed`. Это запас, а не доказанный исторический момент публикации. Старые +6h и примеры issue07Z отменены. Не называй evaluation полностью as-issued/leak-free, пока это не доказано. Пример проверенного покрытия: run00Z, issue12Z, 48h горизонт из 72h источника.

## Команда, владение и связь

- C1, задача `01a0cd56-615f-75d1-b45b-4350146e157f`, ветка agent/c1-data: данные/погода, src/data и src/weather. Задавай конкретные запросы дат/переменных/выборок; не отправляй в новый бесконечный обзор.
- Ты C2: src/ml, src/agent, src/cli и их тесты; технические решения внутри этого scope принимаешь сам. Ведёшь единый журнал API/GPU-бюджета. Можешь поручать существующим коллегам ограниченную помощь в их областях; не захватывай их файлы и не размножай постоянные сессии без необходимости.
- C4/Codex2 на ноутбуке 2, фактическая ветка agent/c4-ui: Python manifest/lock, src/contracts, src/api, scripts, интеграционные проверки, README/демо. Передай зависимости и callable; он подключает транспорт. Не ждать его установки, чтобы создать своё venv и baseline, но передать версии для общего manifest.
- Claude, ноутбук 2, agent/claude-review: после совета frontend/design, владеет web/. Ему нужны реальные поля, события, состояния и пример; не поручай ещё один большой поиск вместо интерфейса.
- C3, задача `01a0cd70-9795-7261-9af4-c0141e92731e`: координатор и единственный интегратор coord/hackalem-start/main. Передавай READY с SHA и командами. Самостоятельные merge в main, изменение общих контрактов или чужих модулей не входят в твою роль.

С доступными задачами связывайся напрямую через инструменты Codex; с ноутбуком 2 — через Git. Каждые 10 минут активной работы и сразу при blocker: commit/push своих изменений, fetch, чтение статусов/адресованных сообщений, ACK. Новые сообщения — уникальные файлы, чужие статусы не менять. Не выдавай отправку за ACK. После 5 минут блокера продолжай независимую часть. Каждое READY содержит наблюдаемый результат и ограничения, а не обещание.

## Совет и исследования

Claude опубликовал recommendation в origin/agent/claude-review: `coordination/council/20260923T141110+0500/decision.md`, shortlist/ledger/roadmap рядом. Это рекомендация совета с ожидаемой ratification A, не подтверждённое одобрение пользователя. Не запускай новый совет и не блокируй обязательную разработку. Направление P0: NWP → проверенный табличный кандидат против baseline → агент → экспорт → один экран; дополнительные идеи только после рабочего пути.

В decision устарело +6h — применять исправление +9h/unconfirmed выше. Все research/DEV1 — reported сведения для адресной проверки, не автоматически доказанные свойства. Не трать час на чтение всего исследования. Просроченные промежуточные сроки не означают сдвиг 18:00.

## NVIDIA и OpenAI: разрешено действовать

Пользователь разрешил до $50 NVIDIA (профиль браузера Magitsian) и отдельно до $50 OpenAI API (Chrome/Google профиль Ash), включая весь бюджет при необходимости. Это командные потолки, не лимиты каждому агенту. Повторное согласие на обычные необходимые запросы в этих пределах не нужно. Реальные balance/service/model access пока не подтверждены: проверь официальную панель, стоимость, квоты и уже понесённые расходы. NVIDIA-аккаунт сам по себе не доказывает наличие Brev GPU-кредитов. Подписка ChatGPT не API-кредит.

Используй доступный авторизованный браузер и профиль согласно computer-use skill/документации инструмента. Если профиль не виден, сообщи конкретный blocker владельцу и продолжай CPU; не обходи авторизацию и не угадывай аккаунт. Не публикуй ключи в Git/чатах/выводе. Локальные secrets/env; .env.example без значений.

Журнал расходов — coordination/resources/C2-budget-ledger.md, один писатель C2. Учитывай текущий и планируемый расход, цену, владельца job, stop deadline; никаких пополнений/подписок/превышений. На настройку GPU максимум 10 минут, CPU-baseline параллельно по независимым шагам. Не более одной платной GPU-машины; остановить compute после эксперимента, проверить отдельно оплату диска. Build/NIM inference не называть GPU-обучением. Не обучать LLM с нуля и не скачивать гигабайты без измеримой пользы.

OpenAI API используй для ограниченного реального tool-calling цикла, анализа предупреждений и пересчёта. Проверь актуальные модели/SDK/цены по официальным docs, поставь timeout, ограничение шагов/вывода/повторов. Передавай сводки вместо всех строк, кэшируй повторяемое. Самая дорогая модель не обязательна; бюджет — возможность, не цель искусственного расходования.

## Сроки и готовность

На момент назначения около 14:36 UTC+5, 23 сентября. Первые 3 минуты CLAIM; первые 10 минут среда, sample, split, сигнатура C4 и запуск baseline. Если 14:45 уже недостижимо, сразу сообщи реалистичный ближайший ETA и сокращай scope, не перенося финальный дедлайн.

15:15 — основной кандидат и честное сравнение; 15:45 — агент+CLI+API; 16:15 — пакет прогнозов/replay и воспроизводимость; 16:15–16:40 — чистый запуск на ноутбуке 2. Обязательная версия и передача на сдачу 17:00, дальнейшее время только проверка/критические исправления, 17:40–18:00 неприкосновенный технический резерв. Финальная отправка принадлежит C3/участнику A; ты передаёшь проверенные артефакты заранее.

Критерий твоего завершения: работающий сквозной расчёт, реальный агентный проход, проверенные временные ограничения, воспроизводимые команды/артефакты, обработка отказа и пересчёта, честный отчёт о качестве/допущениях/расходах, commits переданы и приняты. Не завершай работу после очередного плана. Начинай выполнение сейчас.
