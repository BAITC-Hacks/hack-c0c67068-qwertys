# Текущие задания по уточнению пользователя

Обновление около 14:16 UTC+5: Claude ведёт дизайн и frontend; Codex2 второго ноутбука — универсальный помощник по подготовке и проверке. Этот документ и последнее сообщение DECISION имеют приоритет над старым расписанием ролей, исходными AGENT_BRIEFS и поручением совета. Назначение считается полученным после ACK исполнителя.

## CLAUDE — закончить исследование и перейти к интерфейсу

Ветка остаётся `agent/claude-review`. Сейчас закончить текущий поиск датасетов и источников, опубликовать отдельным commit до 14:25. Для каждого кандидата: ссылка, лицензия, период, переменные, размер, доступность на исторический момент, польза и стоимость интеграции. Выборочные research_notes допустимы; секреты, закрытые CSV и большие архивы не коммитить.

Проверку источников и дальнейшее скачивание передать C1. Не скачивать один полный архив независимо на двух ноутбуках. По последнему опубликованному запросу B совет RUN_ID `20260923T141110+0500` расширен до абсолютного предела14:35; после этого UI. Идеи C1 опубликованы commit `d8ff3ee`, бюллетень `0246028`; получить через fetch. Состав голосующих фиксируется честно.

После 14:31 главная работа — дизайн и `web/`. До 14:50 первый читаемый экран, до 15:20 подключён реальный API, до 15:50 полный сценарий. Один экран диспетчера: турбина, дата выпуска, горизонт 24/48, запуск, график, таблица, экспорт, ход агента, обновление и пересчёт. Явные состояния loading/error/not_ready/saved/live. Не придумывать метрики или физические единицы. До подключения API примеры маркировать как синтетические.

Claude владеет `web/src/`, `web/public/`, дизайном, затем всем `web/` после передачи каркаса помощником. Авторизацию, сложную карту, декоративное 3D, лендинг и общий чат в обязательную версию не включать. QA/README передать C4. Дальнейшие исследования не должны отнимать время интерфейса.

## C4 / Codex2 — окружение, подготовка, API, QA и README

Это помощник на ноутбуке 2. Уже обнаружена его ветка `agent/c4-ui` и CLAIM: сохранить существующую ветку, не создавать agent/c4-support параллельно. Название историческое; новая роль — помощь. Уже сделанный scaffold передать Claude через READY/ACK, затем инвентаризация и API/QA.

До 14:30:

- Проверить Python, Node, Git. Использовать уже установленный совместимый runtime; не обновлять ОС и не ставить WSL/CUDA.
- Подготовить отдельное окружение backend и согласованный manifest/lock. Кандидаты по реальным импортам: pandas, NumPy, pyarrow, scikit-learn, CatBoost, FastAPI, Pydantic, uvicorn, httpx, pytest; SDK только выбранного LLM-провайдера. Ставить отсутствующее, проверять импорт, фиксировать версии. Не скачивать всё из каталогов.
- Согласовать с Claude React/TypeScript/Vite и одну библиотеку графиков. Если Claude уже создал `web/`, не пересоздавать scaffold, не перезаписывать manifest/lock. Если `web/` нет, подготовить только минимальный каркас и команды запуска, отправить READY. После ACK Claude прекращаешь менять JS manifest/lock/config: они переходят Claude.
- Подготовить безопасный `.env.example` с пустыми значениями, инструкции локальных данных и SHA-проверки. Личные ключи не копировать. Исходные CSV получать из официальной выдачи, не через публичный Git.

До 14:40 подготовить каркас FastAPI и схемы с C1/C2/C3, healthcheck, один явно синтетический fixture для UI, команды install/start/test. Пока расчёт не подключён, API возвращает честный not_ready. PROPOSED контракт сначала согласовать, затем реализовать.

После 14:40 подключать транспорт API к функциям C2, проверять сборку и запуск, помогать диагностировать ошибки, писать адресные интеграционные проверки. Чужой дизайн или ML менять только после передачи конкретного файла. README заполнить по фактическому состоянию до 16:15; в 16:15–16:40 выполнить чистый запуск с B.

Владение: Python manifest/lock, `src/contracts/`, `src/api/`, `scripts/`, `tests/integration/`, `docs/verification/`, `docs/demo/`, README после передачи Claude. Сложные решения по данным/модели передавать C1/C2, не подменять их догадками.

Проверить спорные пункты подготовленного Claude QA: clipping в [0,1] только после подтверждения нормализации; число выпусков и время origin согласовать; deterministic fallback не является доказательством живого agentic запуска; кэш в Git только после проверки лицензии, размера и правил данных.

## C1 — подготовить данные и погодный адаптер

Рабочая копия `D:\HACKALEM\worktrees\c1-data`, ветка `agent/c1-data`. Аудит принят, идеи опубликованы. После короткого голоса в совете перейти к подготовке, не начинать новый большой обзор.

До 14:35 сохранить воспроизводимый source-probe, ограниченный локальный кэш проверенных выпусков и отчёт: параметры, SHA, часы, null, происхождение, ограничения available_at. Подготовить часовой образец SCADA с покрытием часа, правилом агрегации и явной гипотезой timezone. До 14:45 дать погодный адаптер и подготовленные входы по согласованной схеме.

Получить карточки источников Claude, проверить пригодность и передать C2. Полное скачивание только под конкретный запрос C2 на обучение/проверку; не тянуть все источники за три года. Соблюдать лимиты провайдера. Неподтверждённое время публикации отмечать предположением. Факт февраля не использовать и не моделировать как настоящий.

Владение: `src/data/`, `src/weather/`, соответствующие тесты и research/C1. Исходные CSV и крупный кэш не коммитить. Модель, UI и общие зависимости самостоятельно не менять. При проблемах установки запросить помощь C4; готовые bundled библиотеки допустимы для первичной проверки, но воспроизводимость проекта требует manifest.

## C2 — модель, прогноз и затем агентный процесс

**Уточнение пользователя: C2 — главный агентный исполнитель решения. Найдена задача `01a0cd9e-371b-77a1-a6fb-62c6869e3bb5` «Начать новую сессию». Полное поручение C2_LEAD_EXECUTOR.md заменяет ниже статус отсутствующего адресата. C3 остаётся интегратором, Claude — frontend, C4 — помощником/API/QA.**

**Новое исполнимое поручение — C2_START_NOW.md. Пользователь разрешил до50USD NVIDIA и отдельно до50USD OpenAI API, см. BUDGET_AUTHORIZATION.md. Оно имеет приоритет над прежним budget UNSET и более ранними сроками первого baseline.**

Третий Codex ноутбука 1 пока не идентифицирован в доступном списке задач. Задание подготовлено, а не автоматически доставлено. Человек A передаёт его реальному исполнителю и получает CLAIM. Ветка `agent/c2-ml`.

Сразу согласовать с C1 нужные даты и погодные переменные, с C4 схему вызова. До 14:40 baseline и временной протокол; до 15:15 проверенный кандидат, исторические метрики и функция прогноза двух турбин на 24/48 часов. Исключить отсутствующие февральские лаги; не сообщать февральские метрики без факта. Сохранять baseline при отсутствии подтверждённого улучшения.

После numeric forecast собрать `src/agent/` и CLI: вызовы weather/data/model/validation/export, ограниченные повторы, реальный журнал, обновление входа и пересчёт. C1 поставляет погодный инструмент, C4 помогает транспортом и обработкой ошибок. До 15:45 полный цикл, до 16:15 последовательные выпуски февраля. API-вызовы только после проверки владельцем доступности и лимита.

Владение: `src/ml/`, `src/agent/`, `src/cli/`, тесты этих модулей. Изменение Python manifest запрашивать C4. Если объём не помещается, передать C4 конкретный plumbing-файл; не размывать владение всем ядром. При отсутствии C2 более 10 минут A назначает фактическую замену.

## C3 — координация и интеграция

Эта задача: назначения, приоритеты, приёмка контрактов и Git-интеграция. Самостоятельно кейс и голосование не реализует. Доступным агентам поручения передаются напрямую; остальным через Git и участников команды. Статусы назначено/доставлено/подтверждено различаются.

Обязательная версия 17:00, резерв 17:40–18:00. Принимать небольшие проверенные READY-коммиты. Не устанавливать зависимости в чужих окружениях и не обещать непрерывное наблюдение без реально запущенного механизма.

## Ограничения подготовки

Скачивать только нужные библиотеки, официальные CSV, небольшие погодные выборки и действительно используемые UI-ресурсы. Перед крупной загрузкой проверить размер и ожидаемое время. Если подготовка занимает больше 10 минут, сообщить блокер и выбрать лёгкий путь.

Не скачивать заранее весь каталог 2295 skills, подборки GitHub, веса LLM, гигабайтные GRIB-архивы. GPU, CUDA, WSL и дополнительные облачные машины не нужны для стартовой подготовки. Проверка NVIDIA-доступа отделена от платного запуска; лимит расходов пока неизвестен.
