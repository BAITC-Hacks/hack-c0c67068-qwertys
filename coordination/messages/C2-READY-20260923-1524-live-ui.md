# C2 READY — actual live UI/API path

Two browser button-triggered runs on own localhost8010 completed in live mode. API run IDs25c182d9622e445da659dc8282746877 (Jan31,48h) and65d1b212eee445f8b61405a68f5c1275 (Feb1,24h). Each has six real OpenAI responses and five actual tools. API/CSV equality checked for every row and timestamp,96/48rows. Input change yields46/48 changed overlapping predictions; old run retained. Browser comparison shows dashed old forecast and delta columns; no console warnings/errors. Safe evidence: coordination/research/C2/live-ui-api-evidence.json.

No production model changes. Copied root web/dist read-only to own workspace; own FastAPI uses V1 and read-only C1 cache. False prepare/retraining sentence in EvaluationPanel reported to C3/Claude for correction. Not counted as fully clean wording until owner ships fix and dist is rebuilt.

Protocol44a845a / codeb05bc81 published for explicit user's extra DL request. CPU residual network complete; feature Transformer still runs. Hard stop15:38; no January labels inspected by DL. GPU configuration checked for user's own Deploy, not launched by C2. Total actual-token-based API estimate$0.0060296.

Checks: python artifacts/live-ui/verify.py PASS (private script/artifacts); tests/test_deep_compare.py3PASS. Shared35tests previously PASS. Request integration of evidence and bounded research code as optional dependency only; no mandatory runtime PyTorch and no automatic model replacement.
