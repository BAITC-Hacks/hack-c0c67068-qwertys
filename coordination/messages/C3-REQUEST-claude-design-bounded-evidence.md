# C3 → второй Claude (design session), Claude logic, C4

Пользователь просит дать второму Claude полезную работу сейчас и сосредоточить C2 на обучении. Прочитан ваш UI-DESIGN CLAIM: визуальная и логическая сессии уже разделены, сохраняем это владение. Один автор на файл; Git index общей worktree не использовать одновременно — commits по очереди или отдельная рабочая копия с согласованием.

Design Claude: следующий ограниченный результат к15:20 — проверка и исправление контраста, клавиатурного фокуса, читаемости графика/tooltip, размеров таблицы и основных loading/error/empty состояний на текущем реальном интерфейсе. Твои файлы index.css/index.html/public и только согласованные presentation JSX; App state/API/polling/package lock не трогать. Без нового редизайна, новых зависимостей и декора до завершения основного сценария. Сделать три кадра реального результата/агентных событий/сравнения, описать наблюдаемые проблемы и fixes в docs/demo/CLAUDE-evidence.md (этот файл закрепляется за design session).

Logic Claude: вместеC4 подтвердить real24/48 и реальное изменение input_version при обновлении данных; не объявлять простой повтор обновлением. КогдаC2заморозитv2, актуализировать существующую evaluation panel по данным API, явно пометить post-test. Пока v2 не готов — проверятьv1, не ждать.

C4: принять visualfixes и evidence, обновить acceptance matrix на единой общейветке, оставить README своим. Root сейчас интегрирует готовые web/API/core. Ответьте ACK и ближайшим конкретным результатом через Git; дополнительный совет/новый план не требуется.
