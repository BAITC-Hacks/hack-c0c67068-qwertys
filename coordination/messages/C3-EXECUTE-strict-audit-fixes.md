# Исправление строгого аудита — 17:01 UTC+5

Пользователь поручил исправить замечания из отдельной задачи «Провести строгий аудит кейса». Основание: локальный отчёт verification/strict-audit-20260923/VERDICT_RU.md, проверенная версия 798bfb9, 23 адресные проверки / 8 FAIL. Это новое поручение имеет приоритет над прежней остановкой изменений; обучение и модельный поиск по-прежнему остановлены.

## Владение узкими исправлениями

- C2: единый temporal guard для прямого callable; input_version с реальным содержимым модели; общий sanitizer JSON-секретов; итог LLM только из проверенных результатов инструментов. Дополнительно явно разрешена узкая замена safe_summary в src/api/main.py на общий импорт. Остальная API-область остаётся C4.
- C1: upstream hourly_units — обязательные фактические единицы ветра, направления и температуры, отказ при неверных/отсутствующих; регрессионные тесты.
- Помощник api_acceptance_review: только форма issue-time рабочего dashboard и регрессионная проверка поддерживаемого 12Z origin, в agent/audit-ui-fix. C3 временно передаёт ему этот узкий участок от Claude. Другие части web остаются Claude; landing agent/claude-frontend не объединять с этими исправлениями.
- C5: только .github/workflows/ci.yml — Python lock/pytest и web install/build/lint без ключей, GPU и внешних датасетов; C3 делегирует этот новый файл отдельно от C4.
- Независимый автор строгого аудита: повтор адресных проверок после SHA от C3, исходный аудит не перезаписывать. Изменённый контракт выбора времени проверить отдельно, не объявлять старое ожидание произвольных часов исправленным без новой проверки интерфейса.
- C3: интеграция, документация закрытых/открытых замечаний и перенос прошедшего проверку результата в main обычным fast-forward push. После переноса проверить удалённый SHA. Действие сдачи на платформе — отдельный факт.

Время исторической публикации погоды, SCADA timezone и отсутствие февральских labels не исправляются выдуманными значениями. Они остаются открытыми ограничениями. Новые интервалы неопределённости, предикторы и смена V1 не входят в срочное исправление воспроизведённых дефектов.
