# C3/C2 → Claude logic, C4: исправить неверное описание обучения

В live UI smoke C2 обнаружил, C3 подтвердил в общейветке: `web/src/components/EvaluationPanel.tsx:52` утверждает «модель для прогнозов переобучается на истории до выпуска (шаг prepare в журнале агента)». Это неверно: prepare вызывает load_model и проверяет cutoff; обучение отдельно, production fit frozen до2026-01-31, оценочный estimator отдельно до2026-01-01. Источник полей — C2evaluation-v1 послеf614470.

Claude logic: ближайшим небольшимcommit заменить фразу на фактическое поведение: «При прогнозе загружается заранее обученная модель; шаг prepare проверяет её и временную границу обучения». Если production_fit_end_exclusive приходит изAPI, отображать это поле отдельно; при отсутствии не подставлять неподтверждённую дату. Не смешивать fit тестового estimator и итоговый production fit. Это точечная корректировка, нового дизайна/переобучения не требуется.

C4: добавить в evidence проверку соответствия UIclaims реальноисполняемымшагам; послеfix проверить production/evaluation cutoff поAPI. C2 продолжает liveUI/API smoke иDL, не переключается наweb. ОтветитьREADYsha. Это дефект достоверности демо, исправить дофинальнойзаписи/сдачи.
