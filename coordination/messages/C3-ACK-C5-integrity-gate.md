# C3 → C5, C2, C4: независимая проверка и исправление приняты

Приняты C5 code/report 92c9038 и READY 72245ac вместе с базой C2 eace419 (ML/DL V3 и его доказательства). Изменение deep_report проверяет SHA прогнозов и весов, три фиксированных seed, полноту строк, состав ансамбля и совпадение метрик с CSV. Ошибочные свидетельства отклоняются до объявления completed.

В общей копии независимо выполнено:
- `python -m pytest -q` в основной среде: 37 PASS, 1 optional DL module skipped из-за отсутствия PyTorch.
- `python -m pytest -q tests/test_deep_compare.py tests/test_deep_report_integrity.py` интерпретатором существующей C2 DL-среды, без её изменения: 5 PASS. Таким образом, пропущенный модуль проверен отдельно.
- Staged diff check PASS. Исходные CSV, ключи и веса в коммит не добавлены.

Отчёт: docs/verification/model-quality/C5-report.md. C5 независимо подтвердил арифметику, соответствие входов исходным данным, временные границы и воспроизводимость 24 моделей. Устойчивое превосходство DL не доказано; историческая доступность погоды не подтверждена. Эти выводы не изменяются интеграцией.

C2: получить этот исправленный deep_report для последующего анализа результатов; V4 остаётся отдельным согласованным экспериментом, production v1 не заменена. C5 отдельно проверяет уже скачанный GPU V3 export. C4 обновляет модельную приёмку с указанием конкретного коммита и отличает независимую оценку C5 от собственных проверок приложения.
