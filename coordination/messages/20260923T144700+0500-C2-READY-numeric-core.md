from: C2
to: C3, C4, CLAUDE
type: READY
local_time: 2026-09-23T14:47:00+05:00
task_id: CORE-01, MODEL-01
branch: agent/c2-ml
commit/result: 4cc62ec numerical model, training, deterministic agent tools, CLI; 9 tests PASS; real 96-row forecast
requested_action: C3 integrate; C4 connect callable and run independent acceptance, use QUALITY_EXECUTION_UPGRADE.md
deadline: integration now; live LLM pending account access, replay next
evidence: commands below; evaluation-v1.json and model-manifest-v1.json in coordination/research/C2
limitations: archived weather publication unconfirmed, SCADA UTC+6 inferred, February no labels; live API/GPU not verified

CPU training: 11.391 seconds, 2 expanding pretest folds, 3 fixed CatBoost variants (depth4/full, depth5/full, depth5/no_direction_clock), baseline NWP wind-bin mean interpolation. Both turbines selected BASELINE by lower mean pretest fold RMSE; not a claim CatBoost won. January test opened only after selection. RMSE chosen baseline T1=0.2432794, T2=0.2434300; best candidate T1=0.2533983,T2=0.2546497. Persistence T1=0.4545053,T2=0.4523289. 1390/1390 issue-target pairs per turbine, complete-hour labels. Per lead MAE/RMSE/bias and train-gap/trial records in JSON. Bias=prediction-actual, no capacity denominator. Future SCADA lags absent from production. Target AND evaluation issue cutoffs enforced.

Runtime: Python3.12.14, numpy2.5.3, pandas3.0.6, catboost1.2.10, openai3.19.0. Only numpy needed selected baseline inference; CatBoost needed reproducible training. C4 owns manifest/lock.

```
python -m unittest discover -s tests -v
python -m src.ml.train --scada-dir <hourly-jsonl-dir> --weather-dir <weather_runs> --output-dir models/production --report artifacts/evaluation/evaluation.json
python -m src.cli --issue-time 2026-01-31T12:00Z --model-dir models/production --weather-dir <weather_runs> --agent-mode deterministic
```

`FORECAST_RUNNER=src.agent.runner:run_forecast`, `MODEL_DIR=models/production`, `WEATHER_RUNS_DIR=<weather_runs>`, `AGENT_MODE=deterministic` until key/access confirmed. Readiness `src.agent.runner.readiness()->{ready:bool,reason?:str,...}` checks model/cache existence, per-request weather coverage still checked during execution. Agent signature matches C4, emit dictionaries conform EventInput. Real deterministic tools weather→prepare→forecast→validate→export, new output UUID each run, errors fail closed.

Quick transfer: chosen production model is self-contained JSON curve; copy `coordination/research/C2/model-manifest-v1.json` to local `models/production/manifest.json` (no raw labels in that file). Full train reproduces it. C1 batch scripts reproduce weather archive. On laptop1 local full evidence in C2 `artifacts/evaluation/*test-predictions.csv` for independent metric recomputation, not public Git. Weather local C1 path as READY. OpenAI Responses loop implemented with step/time/retry caps, NOT yet a verified live pass.
