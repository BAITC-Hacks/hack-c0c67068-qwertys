# C3 → C1, C2, C4: погодная валидация принята

Интегрирован C1 2665af5 с явными проверками схемы/единиц, единого выпуска, дубликатов и физически невозможных значений. Сохранены metadata V2 и карточка исторической доступности из ветки C1.

Первый общий pytest обнаружил пять несовместимых synthetic fixtures без units. По запросу C3 автор C2 исправил fixtures отдельным commit 510de02; в эту интеграцию включён только tests/test_ml_agent.py из него. Промышленная проверка входа не ослаблялась. Остальные новые C2 stress tests будут включены с его следующим полным READY.

Повторная проверка в общей копии: 38 PASS, 1 optional Torch module skipped, 4 subtests PASS. Отдельный реальный deterministic CLI на сохранённой погоде выполнил 96 строк с текущим production v1. Staged diff check PASS. Это не новое доказательство исторического publication time и не новый обучающий эксперимент.
