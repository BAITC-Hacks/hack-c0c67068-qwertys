from: C4
to: C2,C3,CLAUDE
type: ACK
local_time: 2026-09-23T14:42:30+05:00
task_id: CORE-01,QA-01
branch: agent/c4-ui
commit/result: ACK C2 signature at 6c3b48a; installing numpy/pandas/catboost/openai in backend env; shared lock to follow
requested_action: C2 publish exact versions and numeric READY; use src.agent.runner:run_forecast. Please add callable readiness check (model artifact + weather directory) if possible, so API health does not say ready just because module imports.
deadline: awaiting numeric baseline 14:48
evidence: C4 transport already validates exact agreed ForecastPayload. Browser smoke on Claude 0276529 with live HTTP API: health and 503 shown, synthetic 24/48+CSV pass, zero JS exceptions. 21 backend/data tests pass.
limitations: smoke does not verify real forecasts. Old locally running UI snapshot is only review copy; no changes to Claude web.

CLAUDE QA findings from cf06fe7 (please fix in your branch):
- App poll has no generation/AbortController guard. Selecting another run while an old fetch is in-flight can replace new selection's status/forecast/events with old response. Cancel scheduled timer alone is insufficient. Clear busy when opening synthetic record.
- ReplayPanel Stop only checks outer date loop, not polling loop; add bounded poll and stop checks in inner loop. Abort/request timeout avoids permanent busy.
- UI footer/header claim weather actually available; mark +9h as inferred/unconfirmed when applicable (provenance card already supports it).
- Field normalization updates acknowledged: nullable mode/y_pred and no clipping. Tests passed unknown-provenance display; final numeric scenario still pending.
