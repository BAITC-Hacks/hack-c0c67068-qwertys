# V5 frozen bounded GPU calibration batch

Frozen before any V5 fit/results, 2026-09-23 17:17 UTC+5. User explicitly reopens one research cycle. Wallclock hard stop17:27:59; CUDA must be available by17:18:59. Begin provider Stop no later than17:22:45 to reserve the previously measured4m48 shutdown. Training/export deadline17:21:45. One existing Brev T4 47y85unm3 only, <=$2 cycle and shared NVIDIA$50, no top-up. V1 and demo8010 immutable.

Hypothesis: V4 global shrinkage leaves wind-regime-specific bias/high-wind regression. Reuse the exact36 V4 outer MLP checkpoints and fixed selected epochs. Reconstruct only inner-fit models (same seed/epoch/preprocessing) on GPU to obtain inner-stop residual predictions; export those calibration predictions and fitted coefficients for audit. No new network architecture, epoch selection, datasets or January/February labels.

Exactly THREE candidates: ridge pseudo-count lambda32,128,512, in that order. Each uses wind regimes <4,4–8,>=8m/s. If a regime has fewer than100 inner-stop pairs, fall back to V4 global alpha and zero intercept. Otherwise fit residual affine correction alpha*r+b by least squares with ridge penalty lambda*mean(r^2)*(alpha-alpha_global)^2 + lambda*b^2. Clip alpha to[0,1], b to[-.05,.05]. Fit only on inner-stop rows, not outer labels. Same three seeds42/137/2026 averaged equally. No best seed/device selection.

Dataset exactly b4d01f11d33d485c1a36940b849ba345300698fa306ac98ffcb25749d5458dc0. Six V4 temporal folds and seven features unchanged, both turbines, exclude all targets >=2026-01-01 before work. Outer curves/recent91/CatBoost reference predictions taken from verified V4 artifacts, checked against input times/labels. Outer MLP inference is from unchanged V4 weights; new calibration affects inference only.

Acceptance for BOTH turbines: all6folds x3seeds complete; >=2% mean-fold RMSE gain against full curve, recent91curve and fixed GPU CatBoost; no fold or lead1–24/25–48/highwind>=8 slice >5% RMSE regression against either curve; required slice n>=100 else failure; absolute fold bias <=both reference absolute biases+.01. Paired3-target-day block interval (2000replicates, seed20260923) upper endpoint must be negative; use98.333% interval (Bonferroni over3 candidates). All gates frozen; no relaxation. If multiple pass, lowest equally weighted two-turbine mean RMSE qualifies for later independent acceptance, tie favors larger lambda. No automatic production promotion.

These are reused-data research diagnostics, not independent external/industry benchmarks. Historical weather availability and SCADA timezone remain inferred. A no-go/timeout is an explicit incomplete experiment, never a quality win. Save code/input/weights/CSV hashes and CUDA hardware/runtime; export before Stop. No GPU restart after this cycle.
