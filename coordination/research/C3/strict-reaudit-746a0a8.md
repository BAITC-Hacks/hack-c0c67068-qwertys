# Независимая повторная приёмка исправлений

**READY / PASS в пределах согласованных исправлений.**

Проверен неизменный commit **746a0a8af21b803a549eb2dab5a460797ba4d627**, D:/HACKALEM/hack-c0c67068-qwertys. Исходный аудит commit 798bfb9 сохранён. Модель не переобучалась, исторические benchmark-пары не пересчитывались, платные LLM/GPU не вызывались.

## Судьба восьми исходных FAIL

| Проверка | Повторная приёмка |
|---|---|
| Direct callable temporal gate | **PASS**: прежний обход отвергается; допустимая граница +9 ч принимается. Это проверка соблюдения гипотезы, не доказательство исторического времени публикации. |
| Model-content identity | **PASS**: изменение чисел кривой при прежнем model_version изменяет input_version. |
| Неверные upstream units | **PASS**: °F/radian отвергаются до внутренней нормализации. |
| Отсутствующие upstream units | **PASS**: отсутствующие единицы отвергаются, реальные m/s, °, °C принимаются. |
| JSON password redaction | **PASS**: синтетический секрет скрыт; существующие plaintext/Bearer проверки сохранены. |
| JSON token redaction | **PASS**. |
| Ложный текст LLM о качестве/provenance | **PASS**: синтетический ложный текст не появляется ни в одном событии, итог формируется из проверенных данных. Живая LLM заново не вызывалась. |
| Требование всех 24 origin-часов | **RETIRED_CONTRACT**, не PASS: продукт явно сужен до 12:00 UTC. Новый контракт отдельно прошёл браузерную проверку ниже. |

## Новые проверки и регрессии

- **30/30 адресных проверок PASS** в `system-reaudit.json`. Сюда входят семь исправлений, положительные контроли, эквивалентность 12Z/+05/+06, реальные API/CSV/перезапуск, 100 синтетических погодных сценариев внутри одной проверки и сохранность исходного аудита.
- **94 Python-теста и 15 подтестов PASS**, 7,30 с в имеющемся исследовательском окружении.
- **2 Node-теста origin PASS**: `node --test web/tests/issue-origin.test.mjs`.
- **Build PASS**, свежий bundle `index-B-LvBOfW.js`.
- **Lint: 0 ошибок, 6 прежних предупреждений**. Предупреждение о размере bundle также остаётся.
- Сравнение 96 чисел на неизменных исходных входах: **максимальная абсолютная разница 0,0**. Исправления не изменили этот численный прогноз.
- Проверены HEAD и чистота продуктовых путей перед запуском; после проверки HEAD тот же. Исходные артефакты совпадают с SHA-256.

Первый вызов Node-теста содержал ошибочный путь `web/tests/time.test.mjs` и ничего не проверил. После чтения фактического имени запущена корректная команда выше, оба теста прошли. Это ошибка команды проверяющего, не дефект продукта; она отдельно сохранена в evidence.

## Браузерная приёмка нового контракта

Использован отдельный localhost:8018 и копия исходного audit SQLite; исходный журнал не изменён.

1. По deep link открыт старый реально сохранённый failed run `504f54f7b3794d1393fc891960f8c673` с origin **18:00 UTC+5**. Исторический origin отображается честно; форма нового запуска показывает только **17:00 UTC+5 / 12:00 UTC**, выбор часа отключён. Retry и +24h для неподдерживаемого старого запуска отключены.
2. После просмотра старого запуска новый прогноз **24 ч** выполнен в **17:00 UTC+5**, завершён, **48 строк**.
3. Новый прогноз **48 ч** выполнен в том же поддерживаемом origin, завершён, **96 строк**.
4. «Обновить погоду (+24 ч)» даёт **02.02.2026 17:00 UTC+5**, 96 строк; предыдущий результат доступен, видны пунктир и разности общих часов.
5. Февральский replay запущен кнопкой интерфейса: **29/29 completed**, покрытие **672/672 часа на каждой турбине**. Кнопка CSV всех выпусков активна, origin панели — 17:00 UTC+5.
6. Ссылка «Скачать CSV» текущего прогноза проверена через её фактический endpoint: **96 строк, численное равенство API**. Копия — `browser-linked-export.csv`.
7. В проверенных браузерных сценариях **console error/warn отсутствуют**. Ограничение кэша явно написано в интерфейсе, переключение timezone описано как изменение отображения.

Браузерные результаты и конкретные run IDs сохранены в `browser-evidence.json`. API остаётся более общим, чем ограниченная форма; ему не навязывалось не заявленное новое требование отвергать любой другой origin. Проверены UI-запрет и честное описание подготовленного кэша.

## Что не закрыто этим PASS

- Историческая доступность NWP и timezone SCADA остаются **UNCONFIRMED**. Temporal guard обеспечивает согласованность принятого допущения +9 ч, но не заменяет журнал поставщика.
- Февральской измеренной точности по-прежнему нет; отсутствующие labels не созданы.
- Предыдущие замечания по сезонной устойчивости, смещению, интервалам прогноза, повторному исследованию test-периодов и бизнес-применимости не переоценивались.
- `main` на момент передачи результата ещё не переведён координатором. Это отдельная последующая операция; финальная отправка на платформу не проверена.
- CI добавлен в репозиторий; соответствующие локальные команды прошли. Статус hosted GitHub Actions этим аудитом не подтверждён.
- README координатор намерен обновить после этой приёмки; окончательный текст про ограничение origin следует сверить с проверенным интерфейсом. Изменение только документации не требует нового обучения.

Итог означает принятие конкретных исправлений и нового ограниченного UI-контракта. Он не превращает первоначальные 63/100 в 100/100 и не является промышленной сертификацией.

## Evidence

В этом каталоге: `system-reaudit.json`, `run-metadata.json`, `commands-evidence.json`, `browser-evidence.json`, `browser-linked-export.csv`. Проверяющий скрипт: D:/HACKALEM/verification/strict-reaudit-20260923/system_reaudit.py. Предварительные критерии: D:/HACKALEM/verification/strict-reaudit-20260923/ACCEPTANCE.md.

Координатор может продолжить согласованную подготовку main и сдачи. Код продукта проверяющим не изменялся.
