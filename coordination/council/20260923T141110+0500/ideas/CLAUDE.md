# CLAUDE ideas — RUN_ID 20260923T141110+0500

Written before reading any other ideas file. 12 perspectives simulated inside ONE context = **one Claude voice**.
Format: ID | title | what | expected result | acceptance | min (impl+check) | owner | risk/unknown

- CLAUDE-01 | Hourly SCADA layer | 10-min → hourly mean in UTC (SCADA fixed UTC+6 hypothesis), coverage = n/6, drop hours <4/6; flag downtime (p≈0 while ws>5 m/s) | clean hourly target per turbine | row counts + coverage table, no Feb rows | 15+5 | C1 | UTC+6 inferred (R02) → ±1 h sensitivity
- CLAUDE-02 | Issue-time rule + single-run fetch | issue_time = D 00:00 UTC+6 (=D−1 18Z); use latest ecmwf_ifs run with init+6 h ≤ issue_time (→ D−1 12Z run); horizon = valid hours issue+1…+48; cache raw JSON with URL, run, fetched_at | as-issued weather for 29 issues (Jan 31–Feb 28) | assert available_at ≤ issue_time < valid_time on every row; 0 nulls | 20+10 | C1 | provenance/latency of archive (R03)
- CLAUDE-03 | Training set with same distribution as inference | Previous-Runs `ecmwf_ifs_previous_day1/day2` (1 request for 2024-03..2026-01) aligned to hourly SCADA; fallback: loop single-runs 12Z | train/inference mismatch avoided (vs ERA5) | overlap ≥ 15 months × 2 turbines | 15+10 | C1→C2 | R04 day1/2 availability semantics
- CLAUDE-04 | Physics baseline | empirical power curve (binned SCADA ws→p, per turbine) applied to NWP ws × linear MOS bias fix | explainable baseline + fallback when ML fails | backtest MAE vs climatology | 10+5 | C2 | NWP bias +0.5 m/s (REPORTED)
- CLAUDE-05 | GBM main model | LightGBM (already installed) or CatBoost: ws100, ws10, shear, dir sin/cos, temp, lead, hour, month, turbine_id → normalized power, clipped [0,1] | beats power-curve baseline on backtest | rolling backtest table | 20+10 | C2 | none beyond data
- CLAUDE-06 | Honest backtest | rolling-origin daily replay over Nov 2025–Jan 2026 with exactly the Feb pipeline (as-issued runs, no SCADA lags after issue) ; baselines: climatology, persistence-24h, power curve | the only credible quality numbers | MAE/RMSE per turbine × lead bucket (1–24/25–48) | 20+10 | C2 | Single-runs coverage for Nov–Jan (REPORTED yes)
- CLAUDE-07 | Agent orchestrator with typed tools | one loop: check_new_run → fetch_weather → validate (nulls, coverage, available_at) → build_features → predict → analyze (bounds, ramps, disagreement vs baseline, NWP run-to-run delta) → decide (accept / recompute / flag) → report; JSONL event log | real tool calls visible in journal | log shows each tool, inputs summary, outcome, retry | 30+15 | C3 | LLM optional
- CLAUDE-08 | Update-triggered recompute (the case's "повторный расчёт") | at issue D uses 12Z(D−1); agent re-checks at D+6h: 00Z(D) now available → new run_id, diff vs previous, keep both | demonstrable re-run on real newer input | two versions stored, delta chart | 15+10 | C3 | same as CLAUDE-02
- CLAUDE-09 | LLM planner/critic, optional | OpenAI tool-calling if `OPENAI_API_KEY` set, else deterministic policy; LLM only chooses tools + writes RU summary, never numbers | no-key reproducibility + agentic demo | run with and without key both succeed, log says mode | 20+10 | C3 | quota UNSET
- CLAUDE-10 | Offline cache mode | commit cached as-issued weather JSON for the 29 Feb issues + backtest runs (few MB) | experts run without network | `--offline` replay reproduces committed forecasts bit-for-bit | 5+5 | C1 | data-publication policy for weather (open data, fine)
- CLAUDE-11 | Uncertainty bands | LightGBM quantile P10/P90 or conformal residuals by lead bucket from backtest | operator-grade output | empirical coverage on backtest ≈ 80% | 15+10 | C2 | calibration period short
- CLAUDE-12 | Multi-NWP blend | add GFS/ICON/AIFS previous-runs, per-source model + average/stack | typical +5–10% in literature | backtest gain > noise | 30+15 | C1/C2 | Feb availability of those sources unverified for single-runs
- CLAUDE-13 | Operator alerts | NWP-based flags: cut-out risk (ws100>20), icing risk (T<0 & RH>90%), ramp >0.3/h, low-confidence (wide band / run-to-run jump) | practical value criterion | flags present in export + UI | 10+5 | C3 | icing needs RH variable
- CLAUDE-14 | Single dispatcher screen | Streamlit (1 file, fastest) or React: issue picker, turbine, 24/48, chart with band, table, CSV export, agent log, weather provenance | demo-able product | click-through by B from README | 30+15 | C4 | React may slip → Streamlit fallback
- CLAUDE-15 | Submission export | `outputs/forecast_feb2026.csv`: run_id, turbine, issue_time, valid_time, lead_h, y_pred(p50), p10, p90, weather_run, available_at, mode | exactly what jury inspects | schema test + 29×48×2 rows | 5+5 | C3 | none
- CLAUDE-16 | Reproducibility pack | `make`-less one command: `python -m wind.cli replay --offline` + pinned requirements.txt + data path env var + README verified on clean clone by B | 25% criterion | clean-clone run log in docs/verification | 15+15 | CLAUDE/B | data not in repo → README instructs path
- CLAUDE-17 | Timezone self-test | agent tool that re-checks SCADA↔NWP lag (argmax corr over −3…+3 h) at startup and warns if ≠ configured | guards R02 | printed lag + warning path | 10+5 | C1 | none
- CLAUDE-18 | Roadmap only | Chronos-2/TiRex covariate TSFM, farm aggregation in MW once R07 known, live mode with Open-Meteo current runs | future potential (10%) | README roadmap | 0 | — | not today
