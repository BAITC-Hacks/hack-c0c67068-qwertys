# Независимое ревью Workspace — 23.09.2026, 17:53 UTC+5

Результат: **PASS в области анализа изменений и регрессий контрактов**. Блокеров для интеграции в просмотренном коде не найдено. Это не заменяет проверку новой компоновки в браузере и сборку, которые выполняет C3.

Проверенный commit: `525f035fd01a9237501e26535ab11b66197496c6`.
База frontend: `f0d9ea52367f5afe5ed66c8d47414f406ffbda24`.
Проверены все изменённые программные файлы: App.tsx, Workspace.tsx, main.tsx, KpiStrip.tsx, index.css. Продуктовые файлы не изменялись.

- 24 проверки сохранности исходного кода/инвариантов — PASS, детали в source-invariants.json. При сравнении блока update/deeplink/export исключена единственная дополнительная пустая строка; это не исправление продукта.
- `node --test tests/issue-origin.test.mjs`: 2 PASS, 0 FAIL. Файл тестов и time.ts идентичны проверенному коммиту.
- Backend, транспорт и типы API не изменены. Создание запроса сохраняет fixed 12Z; launch проверяет допустимость выпуска. Retry старого неподдерживаемого выпуска и +24 ч после последнего поддерживаемого выпуска остаются заблокированы.
- Загрузка серверной истории и localStorage, polling, защита от устаревших ответов, сравнение прогнозов, +24 ч и экспорт через API сохранены. Новые вызовы focusResult меняют только выбранную панель. История теперь отображает все загруженные записи вместо первых 12.
- Численные графики, таблица, вычисления KPI, события, сведения о модели, происхождении входов и исторических метриках не менялись. В KPI сокращены только подписи.
- ReplayPanel остаётся смонтированным: при навигации меняется hidden, состояние и выполняющийся цикл не сбрасываются. CSS явно сохраняет семантику hidden.
- Синтетические данные остаются маркированными; допущения по доступности погоды и SCADA сохранены в TrustStrip, источнике и строке статуса.

Ограничение: в этой задаче не запускались новый браузерный сценарий, обучение, GPU или платные API. Браузерная проверка, сборка и публикация main принадлежат C3. Неподтверждённые происхождение исторической погоды и часовой пояс SCADA остаются прежними ограничениями модели; этот frontend-коммит их не устраняет.
