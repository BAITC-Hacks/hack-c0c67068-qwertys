# Приёмка финального dashboard SAMAL

23.09.2026, C3. Frontend525f035 с точечной правкой: графики не создаются внутри скрытых панелей; состояние ReplayPanel остаётся смонтированным. Backend746a0a8 и V1 не изменялись.

Проверено в настоящем Chrome через отдельный сервер8012, реальную модель, погодный кэш и отдельную SQLite. Режим deterministic, без платных LLM-вызовов. Основное демо8010 получило ту же сборку UI, модель и история сохранены.

| Сценарий | Результат |
|---|---|
| Пустой экран → запуск24ч | 48 строк, completed |
| Запуск48ч | 96 строк, completed; a71db3bc59b3462ab892ecab50a17f99 |
| Обновление+24ч | 01.02 вместо31.01, новый погодный прогон; a0f24a53c83a4671a4c1ebdf04683356 |
| Сравнение и ведомость | Совмещённые часы, две колонки Δ |
| CSV | Оба48ч результата:96строк, точное равенство чисел API |
| Отображение | UTC/+5, Т1/Т2, источник и видимые допущения |
| Метрики | V1 и V2 открываются; V2 помечена post-test |
| Replay | 29/29;672часа на каждую турбину |
| Stop и восстановление | Остановка на16/29; затем29/29 из сохранённых выпусков |
| Навигация | Разделы и история доступны; replay сохраняется при переключении |
| Граница28февраля | Обновление+24ч disabled |
| Телефон390px | Панели Запуск/Прогноз/Агент работают, ширина документа375px с scrollbar; горизонтального переполнения нет |
| Промежуточная1180px | Панель агента выдвигается, Escape закрывает, переполнения нет |
| Сборка | TypeScript/Vite PASS, Node2/2, lint0errors/10warnings |

Во время приёмки обнаружены предупреждения Recharts при скрытом графике. После правки отображения повторно открыты сохранённые результаты и replay29/29, выполнен переход к прогнозу: новых warnings/errors в проверенном интервале нет. Backend и численные результаты не изменялись. Независимый проверяющий подтвердил24/24 исходных инвариантов и Node2/2 для525f035; его проверка не подменяет эту браузерную приёмку.

Сверка README с официальным DOCX кейса и шаблоном пользователя: название, проблема/пользователь, функции, сценарий, технологии, архитектура, установка, повторяемая проверка, данные/интеграции, ограничения и статус развёртывания представлены. В официальном кейсе README/воспроизводимость оценивается25баллами: команды, зависимости, данные и взаимодействие компонентов описаны. Дополнительные доказательства и метрики сохранены.

Сохраняются исходные ограничения исторической доступности погоды, часового пояса SCADA и отсутствия февральских labels. Полной гарантии отсутствия любых ошибок при всех состояниях эта приёмка не даёт.
