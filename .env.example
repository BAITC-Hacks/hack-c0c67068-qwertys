# Copy to .env locally. Never commit populated secrets or raw datasets.
DATA_DIR=
RUN_STORE_PATH=.local/runs.sqlite3
# Set only after the C2 adapter is available, e.g. src.agent.service:run_forecast
FORECAST_RUNNER=
OPENAI_API_KEY=
OPENAI_MODEL=
NVIDIA_API_KEY=
